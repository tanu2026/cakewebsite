apiVersion: "v1"
kind: "Service"
metadata:
  name: "cakeshop"
  namespace: "cake"
  labels:
    app: "frontend"
spec:
  ports:
    - protocol: "TCP"
      port: 80
      targetPort: 5000
  selector:
    app: "frontend"
  type: "ClusterIP"
